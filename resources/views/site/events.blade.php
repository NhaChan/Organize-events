@extends('layouts.site') @section('title','Bài viết và hình ảnh sự kiện — '.$settings['brand_name']) @section('description','Hình ảnh, mẫu trang trí và bài viết dịch vụ sự kiện của '.$settings['brand_name']) @section('content')<section class="inner-hero"><span>BÀI VIẾT & HÌNH ẢNH</span><h1>Ý tưởng cho ngày vui</h1><p>Xem các mẫu trang trí, tiết mục và sự kiện chúng tôi đã đăng tải.</p></section><section class="party-section"><x-navigation-trail/><form class="search-bar"><input name="q" value="{{ request('q') }}" placeholder="Tìm bài viết..."><select name="category"><option value="">Tất cả dịch vụ</option>@foreach($categories as $cat)<option value="{{ $cat->slug }}" {{ request('category')===$cat->slug?'selected':'' }}>{{ $cat->name }}</option>@endforeach</select><button class="party-btn primary">Tìm kiếm</button></form><div class="post-grid">@forelse($events as $event)<x-event-card :event="$event"/>@empty<div class="empty-state">Chưa có bài viết phù hợp.</div>@endforelse</div>{{ $events->links() }}</section>@endsection
